import styles from "@/assets/styles/Navbar.module.css"; // Importamos el módulo de CSS

export const Navbar = () => {
    return (
        <header>
            <nav className={styles.contenedor}>
                <ul></ul>
            </nav>
        </header>
    );
};
